<Window x:Class="Scada.Wpf.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="History" Height="520" Width="740"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Analog input" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="TagCombo" Width="180" SelectionChanged="TagCombo_SelectionChanged"/>
        </StackPanel>

        <TextBlock x:Name="StatsText" Grid.Row="1" Margin="0,0,0,8" FontWeight="Bold"/>

        <ScottPlot:WpfPlot x:Name="Plot" Grid.Row="2"/>
    </Grid>
</Window>
