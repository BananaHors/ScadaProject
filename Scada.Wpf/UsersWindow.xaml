<Window x:Class="Scada.Wpf.UsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Users" Height="540" Width="440"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Users" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

        <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridTemplateColumn Header="" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="DeleteButton_Click" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Create user" FontWeight="Bold" Margin="0,0,0,6"/>

            <TextBlock Text="Username"/>
            <TextBox x:Name="UsernameBox" Margin="0,2,0,8"/>

            <TextBlock Text="Password"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,2,0,8"/>

            <TextBlock Text="Role"/>
            <ComboBox x:Name="RoleCombo" Margin="0,2,0,8">
                <ComboBoxItem Content="Admin"/>
                <ComboBoxItem Content="Operator"/>
                <ComboBoxItem Content="Student"/>
                <ComboBoxItem Content="Teacher"/>
            </ComboBox>

            <TextBlock Text="Password: 15+ chars with an uppercase, lowercase, and special character."
                       FontStyle="Italic" Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,6"/>

            <TextBlock x:Name="ResultText" TextWrapping="Wrap" Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Create" Width="90" Click="CreateButton_Click"/>
                <Button Content="Close" Width="90" Margin="10,0,0,0" Click="CloseButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
